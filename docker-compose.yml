version: '3.1'

services:
    node:
        build:
            context: .
            args:
                - NODE_ENV=development
        command: node_modules/.bin/nodemon -e html,js,css --inspect=0.0.0.0:9229
        ports:
            - 80:80
            - 5858:5858
            - 9229:9229
        volumes:
            - .:/opt/app
        environment:
            - NODE_ENV=development
            - COOKIE_HASH=gSha8kecFe2rCS8eGAughJfbwsfEEdDYykhA5QRdJjxfPK42
            - USER_API_BASE=https://iopwyeiagc.execute-api.us-east-1.amazonaws.com/dev
            - REMOTE_API_BASE=https://342atck0ae.execute-api.us-east-1.amazonaws.com/dev
